# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Design source files — live in Figma / Sketch cloud, not the repo
*.psd
*.sketch
*.fig
*.xd
*.ai

# Local previews / build leftovers
_site/
.cache/

# Logs
*.log

# Heavy raw assets — commit only the optimized web versions
*.tiff
*.raw
*.heic
